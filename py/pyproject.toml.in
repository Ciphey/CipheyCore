[tool.poetry]
name = "cipheycore"
version = "@CIPHEY_VERSION@"
description = ""
authors = ["Cyclic3 <cyclic3.git@gmail.com>"]
build = "build.py"

[tool.poetry.dependencies]
python = "^3.5"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[build-system]
requires = ["poetry>=0.12", "setuptools-cpp"]
build-backend = "poetry.masonry.api"
